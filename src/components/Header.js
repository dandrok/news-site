import StyledButton from './UI/StyledButton'

const Header = () => {
  return (
    <>
      <section>
        <StyledButton>Hello world</StyledButton>
      </section>
    </>
  )
}

export default Header
